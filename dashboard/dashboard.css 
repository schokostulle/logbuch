/* /logbuch/dashboard/dashboard.css — Version 0.3 */

/* =========================================
   DASHBOARD-INHALT
   Ergänzt das globale Layout (style.css)
========================================= */

/* Grundstruktur */
#content {
  padding: 20px 24px;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  justify-content: flex-start;
  gap: 12px;
  box-sizing: border-box;
}

/* Überschrift */
#content h2 {
  margin-top: 0;
  margin-bottom: 8px;
  font-size: 1.6em;
  font-weight: 600;
}

/* Standardabsätze */
#content p {
  line-height: 1.5;
  max-width: 800px;
}

/* Platzhalter oder Infoelemente */
#content .info-box {
  border: 1px solid #ccc;
  padding: 12px 16px;
  border-radius: 8px;
  background: #fafafa;
  width: 100%;
  max-width: 900px;
  box-sizing: border-box;
}

/* Tabellen oder Toolcontainer (Vorbereitung für spätere Module) */
#content .tool-container {
  width: 100%;
  max-width: 1000px;
  overflow-x: auto;
  border: 1px solid #ddd;
  border-radius: 6px;
  background: #fff;
  padding: 8px;
  box-sizing: border-box;
}